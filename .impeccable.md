# Impeccable · 设计上下文

> 2026-08-17 建。内容来自项目实际情况（代码里的品牌色映射、使用者、内网环境）与长期协作中已确认的事实。
> 有新的品牌规范或用户反馈推翻这里的判断，直接改这份文件。

## Design Context

### Users
岛津（Shimadzu）上海设计室的 5 位设计师（Sun / Jiang / Zhang / Xu / Lii）。公司内网、白天、桌面浏览器为主，偶尔手机看。

他们要办的事有三件，频次从高到低：
1. **扫**——今天/这几天有没有人传了新东西（碎片时间刷一眼）
2. **翻**——为手上某个项目找参考，按分类、按人、按后续动作定位
3. **录**——把自己看到的东西传上来，写清楚「为什么值得看」

这不是一个 marketing 页面，是同事每天要用的工具。**内容（图片和标题）是主角，界面本身必须退让。**

### Brand Personality
克制 · 专业 · 不喧宾夺主。

品牌色是岛津红 `#c0392b`（代码里通过覆盖 `--color-indigo-*` 实现，所以源码写 `indigo` 实际渲染成红——**改配色前先看 `globals.css` 第 79 行起的映射**）。红色是稀缺资源：只用于「当前选中」和「新建」这一个主行动，其余一律 slate 中性。

情绪目标：打开就能扫，找得到，不添乱。不需要惊喜，需要顺手。

### Aesthetic Direction
浅色。理由不是「保险」，是内容决定的——这个库里全是图片和截图，深色底会跟缩略图打架，而且使用场景是白天办公室。

材质语言（已成型，新组件必须归到其中一类）：
- **白卡 + slate-200 描边 + 极轻投影**：内容和控件的默认底子
- **实色填充 + 白字**：选中态（品牌红）、后续动作标签（四色，对比度已验，见 decisions D0-09）
- **圆角 xl（12px）**：容器；**圆角 lg（8px）**：容器内的小块

反模式（这个项目里明确不要的）：
- 玻璃拟态、发光边框、深色底霓虹
- 彩色竖条描边（`border-left: 3px solid ...`）
- emoji 套圆圈加投影
- 装饰性图标堆在标题上方

### Design Principles
1. **层级靠材质，不靠加大**——两个控件要分主次，先换材质（凹槽 vs 平贴、抬起 vs 填充），不要靠字号和颜色硬凹。
2. **红色是稀缺资源**——选中态和主行动之外不出现红色。红色到处都是等于哪里都不重要。
3. **每个筛选都带数字**——筛选器不只是开关，是「这里有多少东西」的仪表。为 0 的选项要让人一眼看出不用点。
4. **标签不能说谎**——写「本周」就得是本周。做不到就改标签，别让用户自己去猜口径。
5. **一个功能一个入口**——同一件事不给两个按钮（收藏 Tab 定下来时删掉了头像旁的红心，见 decisions D0-17）。
